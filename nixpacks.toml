# Nixpacks configuration for Railway
# This tells Railway to build the server directory

[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['cd server && npm install']

[phases.build]
cmds = ['echo "No build step needed"']

[start]
cmd = 'cd server && npm start'
